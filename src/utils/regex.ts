export const cppClassNameRegex = /^[a-zA-Z_]\w*$/;

// TODO: check acceptable values
// I am not sure how much of c++ syntax they accept
// So I keep it simple for now
export const ofpIntegerRegex = /^-?\d+$/;
export const ofpFloatRegex = /^-?\d*\.?\d+$/;
